// Dates de dernière modification des documents, indexées par chemin dans le dépôt.
// Ce fichier est REGÉNÉRÉ à chaque déploiement par .github/workflows/deploy.yml
// (git log par fichier). La version commitée ici sert de repli en local :
// sans elle, les dates sont simplement absentes, rien ne casse.
window.DOC_DATES = {};
